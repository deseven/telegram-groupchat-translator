version: "3.9"
services:
  bot:
    image: node:18-alpine
    working_dir: /app
    volumes:
      - .:/app
    command: sh -c "npm install && npm run start"
    env_file:
      - .env
    ports:
      - "${PORT}:${PORT}"
